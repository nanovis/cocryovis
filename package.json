{
  "name": "@cocryovis/workspace",
  "private": true,
  "workspaces": [
    "client",
    "schemas",
    "server"
  ],
  "scripts": {
    "build": "npm run -w schemas build && npm run -w server build && npm run -w client build",
    "tscheck": "npm run -w schemas tscheck && npm run -w server tscheck && npm run -w client tscheck",
    "lint": "npm run -w schemas lint && npm run -w server lint && npm run -w client lint",
    "format": "npm run -w schemas format && npm run -w server format && npm run -w client format"
  },
  "engines": {
    "node": ">=22"
  }
}
